PORT=5000
MONGODB_URI=mongodb://localhost:27017/venue
JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRES_IN=7d
OTP_EXPIRES_MIN=10
CLIENT_URL=http://localhost:5173

# Nodemailer (for OTP emails) — use Gmail App Password or Mailtrap for dev
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_pass
SMTP_FROM=noreply@venue.app
